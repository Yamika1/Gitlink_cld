@model List<POE_CLOUD1.Models.Product>

@{
    ViewBag.Title = "Upload and preview files";
}

<div class="container mt-4">
    <h2 class="text-primary">Upload a File</h2>

    <form asp-action="Upload" asp-controller="Product" method="post" enctype="multipart/form-data" class="mb-4 p-3 border rounded bg-light shadow-sm">
        <div class="mb-3">
            <label for="file" class="form-label">Select a file: </label>
            <input type="file" name="file" id="file" class="form-control" />
        </div>
        <button type="submit" class="btn btn-primary">Upload</button>
    </form>

    <h3 class="text-success">Uploaded Images</h3>
    @if (Model != null && Model.Count > 0)
    {
        <ul class="list-group">
            @foreach (var product in Model)
            {
                if (!string.IsNullOrEmpty(product.ImageURL))
                {
                    <li class="list-group-item">
                        <a asp-action="DownloadFile"
                           asp-controller="Product"
                           asp-route-fileName="@System.IO.Path.GetFileName(product.ImageURL)"
                           class="text-decoration-none">
                            @System.IO.Path.GetFileName(product.ImageURL)
                        </a>
                        - @product.ImageURL
                    </li>
                }
            }
        </ul>
    }
    else
    {
        <p class="fst-italic text-muted">No files uploaded yet.</p>
    }

    <div class="mt-4">
        <a asp-action="Index" asp-controller="Product" class="btn btn-secondary">Back to List</a>
    </div>
</div>
